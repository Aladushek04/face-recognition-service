{
  "name": "face-recognition-service",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "desktop:dev": "npm --prefix desktop/electron run dev",
    "desktop:start": "npm --prefix desktop/electron run start",
    "desktop:install": "npm --prefix desktop/electron install",
    "desktop:build": "npm run frontend:build && npm --prefix desktop/electron run dist:portable",
    "backend:build": "python scripts/build_backend_exe.py",
    "desktop:build:bundled": "npm run backend:build && npm run desktop:build",
    "frontend:build": "npm --prefix frontend run build"
  }
}
